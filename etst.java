
/**
 * Write a description of etst here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
import java.util.Scanner;
public class etst {
    public static int getArray(Scanner input, int size){
        int[] temp = new int[n];
        for(int i = 0; i<size; i++)
            temp[i] = input.nextInt();
        return temp;
    }
}
